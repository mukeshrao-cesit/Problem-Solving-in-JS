// exporting the getItem and setItem functions separately and combined inside and object.

export const getItem = () => {
  return "Hello all";
};
export const setItem = (value) => {
  return value;
};

export default { getItem, setItem };
